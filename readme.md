# https://roadpeoples.org

## local setup

`yarn install`

`yarn dev`

visit `http://localhost:3000`

## Production

`cp .env.example .env`
change parameters in `.env` to sensible values.
`sudo bash setup.sh`
